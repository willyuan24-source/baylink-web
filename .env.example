# 本地开发默认值。不要将数据库、JWT、短信、AI 等服务端密钥放入 VITE_ 变量。
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000

# Vercel 的帖子分享函数读取此服务端变量。生产默认值如下。
# BAYLINK_PUBLIC_API_URL=https://baylink-api.onrender.com/api
